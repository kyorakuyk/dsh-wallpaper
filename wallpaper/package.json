{
  "name": "dsh-wallpaper-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "鲸鱼娘交互壁纸 — 独立前端（不依赖 DSH Web shell）",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:lite": "tsc -b && vite build --mode lite",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "tauri": "tauri",
    "tauri:build:lite": "tauri build --config src-tauri/tauri.lite.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-dialog": "^2.7.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
